# Feishu Job Tracker Agent Entry

Use `SKILL.md` as the canonical instructions. Run all operations through:

```bash
python3 scripts/tracker.py
```

Always dry-run and ask for confirmation before writing to Feishu/Lark.

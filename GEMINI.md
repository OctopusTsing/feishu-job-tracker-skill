# Gemini CLI Instructions

This skill manages a Feishu/Lark Base job tracker. Read `SKILL.md` first. Use:

```bash
python3 scripts/tracker.py <command>
```

Never write without a dry-run and explicit user confirmation.
